{
  "name": "bazar-pos-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "prisma generate --schema=./server/prisma/schema.prisma",
    "build": "prisma generate --schema=./server/prisma/schema.prisma && cd client && npm install && npm run build",
    "vercel-build": "prisma generate --schema=./server/prisma/schema.prisma && cd client && npm install && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "multer": "^2.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^2.2.0",
    "@types/node": "^20.14.0",
    "prisma": "^5.18.0",
    "typescript": "^5.4.5"
  }
}
